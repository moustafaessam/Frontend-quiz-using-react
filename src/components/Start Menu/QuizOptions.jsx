import styles from "../../styles/Start Menu Styling/QuizOptions.module.css";

function QuizOptions({ children }) {
  return <div className={styles.quizOptions}>{children}</div>;
}

export default QuizOptions;
